node_modules/
.expo/
out/
*.log

# ios/ IS committed on purpose — the issues only reproduce with a native directory in
# the repo. Only build output is ignored here.
ios/build/
ios/Pods/
ios/*.xcworkspace/xcuserdata/
.repack-tool/
